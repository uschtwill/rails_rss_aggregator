ul
  li
    h4 id = "headline"
      =entry.title
    p id = "datetime"
      =entry.published
      br
      =entry.author
    p id = "entries"
      =raw HTML_Truncator.truncate(entry.content, 200)
      =link_to 'Read More', "/entries/#{entry.id}"
      br
      =link_to 'Add to your Bookmarks', "/users/#{current_user.id}/entries/#{entry.id}", :method => :post
      br
      | Originally posted at: 
      =link_to entry.url, entry.url

    / - if (entry.user.id == current_user.id)
    /   img.padding-top-50px src="/bookmarks.png"
    / - else
    /   p
    /     | not yet bookmarked
      
    hr
